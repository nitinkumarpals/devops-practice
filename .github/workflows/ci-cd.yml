name: Hello World Workflows

on:
    push:
        branches:
            - main

jobs:
    hello-job:
        runs-on: ubuntu:latest

        steps:
            - name: Say hello
              run: echo "Hello $USER"

            - name: Show date
              run: date